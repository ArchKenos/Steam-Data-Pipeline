[project]
name = "steam-data-pipeline"
version = "0.1.0"
description = "ETL pipeline that ingests Steam game data from SteamSpy API,models it into a star schema using DuckDB, and serves it for analytical queries."
readme = "README.md"
requires-python = ">=3.14"
dependencies = [
    "pandas>=3.0.2",
    "requests>=2.33.1",
]
